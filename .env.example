# Operator-only Python compatibility fallback.
# The typed OpenClaw plugin uses plugins.entries.nango-tools.config and
# OpenClaw SecretRefs instead of reading these variables.
# Do not commit real secrets.

NANGO_PROXY_URL=
EVOLUTION_PROJECT_ID=
EVOCLAW_ID=
CLOUDRU_API_KEY=
