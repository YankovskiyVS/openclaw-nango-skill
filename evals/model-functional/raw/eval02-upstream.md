# Transcript

Skill path: `git:12903d917509fab4a4da4d7dd0489a42c00286e6:skills/amocrm-crm/SKILL.md`

Prompt:

> Нужно получить сделки-лиды из amoCRM, но максимум 3 страницы и максимум 120 элементов. Если записей больше, результат должен честно сказать, что остановился по лимиту. Ничего не выполняй. Покажи выбранный skill, короткий план, JSON предлагаемого tool call либо null, необходимость approval, проверку результата и действия при неопределённом результате.

Answer:

```json
{
  "selected_skill": "amocrm-crm",
  "plan": [
    "После отдельного разрешения проверить наличие OAuth-подключения amocrm-crm и обязательных переменных окружения, не выводя секреты.",
    "Последовательно запрашивать GET api/v4/leads с limit=40 и page=1, 2, 3; следующую страницу запрашивать только при подтвержденном продолжении и остановиться раньше, если данные закончились.",
    "Объединить не более 120 лидов и не делать четвертый запрос. Если после третьей страницы подтверждено продолжение, явно сообщить, что выдача остановлена по лимиту 3 страниц/120 элементов.",
    "Сейчас ничего не выполнять: tool_call ниже — только предлагаемый первый вызов; для страниц 2 и 3 меняется лишь значение page."
  ],
  "tool_call": {
    "tool": "exec",
    "arguments": {
      "command": "python3 {baseDir}/scripts/nango_proxy.py call amocrm-crm api/v4/leads --method GET --query 'page=1&limit=40' --json-output"
    }
  },
  "approval": "Требуется явное новое разрешение пользователя перед любым вызовом, потому что текущая инструкция — ничего не выполнять. После такого разрешения отдельный destructive-approval не нужен: это read-only GET.",
  "verification": "Для каждой страницы проверить HTTP 2xx, JSON и массив лидов в ожидаемой структуре amoCRM; считать фактически полученные элементы и не превышать 3 ответа или 120 элементов. Продолжение считать подтвержденным только по надежным метаданным пагинации ответа. Если после page=3 есть следующая страница, вернуть явное сообщение: «Остановлено по лимиту: получено не более 120 элементов с 3 страниц, в amoCRM есть еще записи». Если продолжения нет до достижения лимита, отметить результат полным. Page=4 не запрашивать.",
  "on_unknown": "Немедленно остановиться, не объявлять выдачу полной и не делать дополнительных запросов. Вернуть уже полученную часть с количеством страниц и элементов, complete=false и truncated=null, а также точную причину неопределенности. Если третья страница полная, но надежного признака продолжения нет, сообщить: «Достигнут лимит 3 страниц/120 элементов; наличие дополнительных записей не удалось подтвердить». Для 401 указать проблему API key/IAM без альтернативной авторизации; для 404 — проверить EVOCLAW_ID; для upstream 4xx/5xx — попросить переподключить amocrm-crm в консоли.",
  "refusal_reason": null
}
```
